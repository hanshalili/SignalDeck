name: "signaldeck"
version: "1.0.0"
config-version: 2

profile: "signaldeck"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  signaldeck:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
